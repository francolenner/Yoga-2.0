dir

git help
git status => Mostra qual é o status atual do nosso repositório
clear => Limpa o terminal

git config --global user.name "seu nome entre aspas"
git config --global user.email "seu email entre aspas"

git config --list

git init => Indica um novo repositório na pasta que eu estou
git add => adiciona arquivos para serem comitados
git add . => adiciona TODOS os arquivosde uma vez só

git commit
git commit -m "titulo do comit entre aspas"

git push
git push -u origin master => será usado no primeiro commit, depois é só utilizar o "git push"